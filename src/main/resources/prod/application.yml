spring:
  datasource:
    url: jdbc:mysql://47.105.86.107:3306/gemini?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=round&allowMultiQueries=true&useSSL=false&serverTimezone=UTC
    username: root
    password: geminiWY@2020
    driver-class-name: com.mysql.cj.jdbc.Driver
    #配置druid
    type: com.alibaba.druid.pool.DruidDataSource
  redis:
    # 数据库索引，默认0
    database: 0
    # 服务器IP地址
    host: 47.105.86.107
    # 连接端口
    port: 6179
    # Redis服务器连接密码（默认为空）
    password: wym123
    timeout: 2000ms
    jedis:
      pool:
      # redis 连接池最大连接数（使用负值表示没有限制）
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

